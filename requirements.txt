# ═══════════════════════════════════════════
# Garud-Drishti / Cryptix — Python dependencies
# Consumed by setup.py (install_requires).
# Frontend: see frontend/package.json
# ═══════════════════════════════════════════

# --- API (garud_drishti/backend) ---
fastapi>=0.109.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# --- Correlation & ingestion (pandas, graph) ---
pandas>=2.0.0
numpy>=1.24.0
networkx>=3.0

# --- Anomaly / temporal detection (detectiontemp, scripts) ---
scikit-learn>=1.3.0
pyod>=1.1.0
tsfresh>=0.20.0
shap>=0.43.0

# --- Optional ES indexing (graceful fallback if missing) ---
elasticsearch>=8.0.0

# --- Config & HTTP (YAML configs, Ollama client) ---
PyYAML>=6.0
requests>=2.28.0
